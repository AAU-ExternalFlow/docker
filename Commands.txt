#Building the image
docker image build -t externalflow .

#Running docker container with local volume at c:/tmp/uploads
docker container run -v C:/tmp/Uploads:/app/uploads -p 8050:8050 --rm externalflow